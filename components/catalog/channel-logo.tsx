'use client';import {useState} from 'react';import Image from 'next/image';import type {Channel} from '@/types/promo';
export function ChannelLogo({channel,size='md'}:{channel:Channel;size?:'sm'|'md'}){const[failed,setFailed]=useState(false);const box=size==='sm'?'h-9 w-14':'h-12 w-20';return <span className={`flex ${box} shrink-0 items-center justify-center rounded-lg border border-navy-100 bg-white p-1.5`} aria-label={`Logo ${channel.name}`}>{channel.logoPath&&!failed?<Image alt={`Logo ${channel.name}`} className="h-full w-full object-contain" height={40} src={channel.logoPath} width={72} onError={()=>setFailed(true)}/>:<span className="text-xs font-black text-navy-500" aria-label={`Logo indisponible pour ${channel.name}`}>{channel.logo||channel.name.slice(0,2).toUpperCase()}</span>}</span>}
