# Registre d'assets enseignes

Déposer les fichiers officiels sous `<groupe>/logo.svg` conformément au manifest `lib/retailers/repository.ts`. Aucun logo n'est reproduit ou téléchargé par le prototype : tant qu'un fichier manque, `RetailerLogo` utilise son fallback accessible.
